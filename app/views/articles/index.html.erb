<div class="container">
<% @articles.each do |article| %>
  <div class="article">
    <h2><%= link_to article.title, article_path(article.id) %></h2>
    <p> <%= article.content[0,200] %>
  </div>
  <% end %>
</div>
